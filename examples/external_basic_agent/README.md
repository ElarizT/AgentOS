# External Basic Agent

This project demonstrates the preview `agentos.toml` manifest format for an
external basic Python agent. Agent OS can validate the project, but external
agent execution is not supported yet.
